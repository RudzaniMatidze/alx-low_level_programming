#ifndef MACROLIKE_HEADER
#define MACROLIKE_HEADER
/**
 * File: 3-function_like_macro.h
 *
 * Desc: header file that compute abs value.
 *
 * @x: input value (an integer)
 */
#define ABS(x) (((x) < (0)) ? ((x) * (-1)) : (x))
#endif
