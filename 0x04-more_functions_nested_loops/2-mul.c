#include "main.h"
/**
 * mul - Functionthat multiplies two integers.
 * @a: first num to multiply
 * @b: second num to multiply
 * Return: addition of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
